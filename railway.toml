[build]
builder = "NIXPACKS"

[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.postinstall]
cmds = ["playwright install chromium"]

[phases.start]
cmd = "python bot.py"
